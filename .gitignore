# openssl EC PRIVATE signing key — must NEVER be committed. It lives only as
# the GitHub Actions secret APK_SIGN_KEY. (keys/openwrt-feeds.pem IS committed.)
/private-key.pem

# local build / SDK / site output (regenerated by CI)
/site/
/sdk/
/bin/
/build_dir/
/staging_dir/
/tmp/

# stray index/package files from local builds
*.apk
packages.adb
packages.adb.sig
packages.adb.asc
index.json

# editor / OS
.DS_Store
*.swp
